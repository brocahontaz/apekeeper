export function relativeTime(value?: string | Date | null): string { if (!value) return 'never synced'; const seconds = Math.max(0, (Date.now() - new Date(value).getTime()) / 1000); if (seconds < 60) return 'just now'; if (seconds < 3600) return `${Math.floor(seconds / 60)}m ago`; if (seconds < 86400) return `${Math.floor(seconds / 3600)}h ago`; return `${Math.floor(seconds / 86400)}d ago`; }
export const number = (n?: number) => new Intl.NumberFormat().format(n ?? 0);

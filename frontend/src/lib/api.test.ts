import { describe,it,expect,vi } from 'vitest'; import { api,ApiError } from './api'; import { get } from 'svelte/store'; import { sessionExpired } from './stores'; describe('api',()=>it('marks session expired on 401',async()=>{vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(JSON.stringify({error:'unauthenticated'}),{status:401})));await expect(api('/api/dashboard')).rejects.toBeInstanceOf(ApiError);expect(get(sessionExpired)).toBe(true)}));
